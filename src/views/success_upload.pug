extends layout.pug

block content
    div(style='display: flex; flex-direction: column; align-items: center;')
        h1 Thanks, pets uploaded
        button#comeback.waves-effect.waves-light.btn(onclick="window.location.href = '/'") OK